export const metadata = {
  title: "Live Demo",
  description:
    "Experience Skawk AI voice calling live. Call our demo number or watch the interactive simulation. See structured JSON extracted from a real conversation.",
  alternates: {
    canonical: "https://skawk.io/demo",
  },
};

export default function DemoLayout({ children }: { children: React.ReactNode }) {
  return children;
}
